{
  "name": "cypress-pipeline",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:test": "cypress run --env ENV=test",
    "report:generate": "mochawesome-merge reports/*.json -o reports/merged_report.json && marge reports/merged_report.json --reportDir reports --reportFilename merged_report.html"
  },
  "author": "lucas sordi",
  "license": "ISC",
  "dependencies": {
    "mochawesome-report-generator": "^6.2.0",
    "cypress": "^13.15.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0"
  }
}
